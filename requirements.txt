# VisionDesk AI — core runtime
# Install CUDA PyTorch for your GPU first: https://pytorch.org
#   pip install -r requirements.txt

ultralytics>=8.3.0
supervision>=0.22.0
opencv-python>=4.8.0
numpy>=1.24.0
mss>=9.0.0
PyYAML>=6.0
gradio>=4.44.0
torch>=2.1.0
Pillow>=10.0.0
transformers>=4.45.0
accelerate>=0.33.0

# Deep reasoners (optional, mutually exclusive in VRAM):
# - LocateAnything-3B: use that project's env, or set LOCATE_ANYTHING_PATH
# - Mage-VL: needs a recent transformers (upstream docs suggest >=5.7 when available).
#   If versions conflict with LocateAnything, use separate venvs or pin one backend.
#   Set MAGE_VL_PATH to a local checkout, or leave model_path as microsoft/Mage-VL.
